org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8
# The app is Java only. Without this AGP 9 applies its built-in Kotlin support
# and pulls the Kotlin toolchain into a build that compiles no Kotlin.
android.builtInKotlin=false
